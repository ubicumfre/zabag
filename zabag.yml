---
- name: Get Hard Drive Model
  hosts: all
  gather_facts: yes
  tasks:
    - name: Extract and display hard drive model
      debug:
        msg: "{{ ansible_devices | dict2items | map(attribute='value.model') | list }}"
